# BGM Library

Drop royalty-free background music files here (`.mp3`, `.m4a`, `.wav`,
`.flac`, `.ogg`, `.aac`). They are picked up automatically when the backend
container starts (`python manage.py seed_bgm`) and become selectable in the
dashboard's BGM dropdown.

Keep only audio you have the rights to use.
